Normally the .ENV file is ignored in gitignore so people don't have access 
to the database information, in this case for the Final Project to be available.

Declaring the variable called uri and assigning the MongoDB connection string using the following code:

MONGODB_URI=MONGODB_URI='mongodb+srv://admin:admin@cluster0.wu8hh65.mongodb.net/Ger-Garage?retryWrites=true&w=majority&ssl=true'
PORT=3001
